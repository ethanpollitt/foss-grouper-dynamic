== grouper.c ==
* Complete the Red-Black tree code
* Create data structure for ID's
* create varible to track ID where "new" rules start
* Write code so that "class" number returned instead gets ID from new data structure to print
  - this code must check if null, and if it is, then get the next "class" match (and repeat check)
* Write code that will process deletes from input file
* Write code that will process inserts from the input file

== big_test.py ==
